document.addEventListener("DOMContentLoaded", () => {
  // small friendly greeting (remove before demo if you don't want the popup)
  // alert("Welcome to Wania's portfolio!");
  console.log("Portfolio loaded");
});
